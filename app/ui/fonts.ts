import { Inter, Lusitana, Bricolage_Grotesque } from 'next/font/google';

export const body = Inter({ subsets: ['latin'] });
export const heading = Lusitana({ subsets: ['latin'], weight: ['400', '700'] });
export const display = Bricolage_Grotesque({ subsets: ['latin'] });
